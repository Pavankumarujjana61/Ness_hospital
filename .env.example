# Server Configuration
PORT=5000
NODE_ENV=production

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=neshosp

# Or provide a single cloud database URL (overrides separate DB configs on Render/Railway)
# DATABASE_URL=mysql://user:password@host:port/dbname

# JWT Secret Key
JWT_SECRET=newlife-super-secret-jwt-key-2026
